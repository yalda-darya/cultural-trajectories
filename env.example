# Copy to .env and fill in. NEVER commit .env (it is gitignored).
# Load in a notebook with python-dotenv, or export in the shell before running.

# Anthropic — Claude Opus 4.8   (console.anthropic.com)
ANTHROPIC_API_KEY=

# OpenAI — GPT-5.5
OPENAI_API_KEY=

# Google — Gemini 3.6 Flash   (either name works; set one)
GEMINI_API_KEY=
# GOOGLE_API_KEY=

# Together — Qwen3.6-Plus
TOGETHER_API_KEY=

# Meta — Muse Spark (evaluated, excluded from panel; keep only if reproducing that check)
# MODEL_API_KEY=
